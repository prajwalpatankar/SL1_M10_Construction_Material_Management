<% include partials/_header %>

    <div class="container container-pm">
        <br><br><br><br><br><br><br><br><br>
        <div class="form-pm">
            <h5>Client Name : <%= client_det.CLIENT_NAME %></h5>
            <h5>Phone Number :
                <% if (contact_det) { %>
                <%= contact_det.PHONE_NO %> </h5>
            <% } else { %>
            - </h5>
            <% } %>
            <h5>Email id :
                <% if (contact_det) { %>
                <%= contact_det.EMAIL %> </h5>
            <% } else { %>
            - </h5>
            <% } %>
        </div>
        
        <br> <br>
        <table class="table thead-light">
            <tr>
                <th>ORDER NO.</th>
                <th>SUPPLIER </th>
                <th>MATERIAL</th>
                <th>QUANTITY</th>
            </tr>
            <tr>
                <td> <%= order_det.ORDER_NO %> </td>
                <td> <%= material_det.MATERIAL_NAME %> </td>
                <td> <%= material_det.MATERIAL_TYPE %> </td>
                <td> <%= order_det.QUANTITY %> </td>
            </tr>
        </table>
        <br>
        <hr>
        <h4 style="text-align: center;">Total Bill Amount : <%= order_det.AMOUNT %> </h4>

    </div>

<% include partials/_footer %>